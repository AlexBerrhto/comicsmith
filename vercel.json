{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "functions": {
    "api/llm.js": { "maxDuration": 30 },
    "api/generate.js": { "maxDuration": 60 }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}